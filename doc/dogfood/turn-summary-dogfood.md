# Turn-Summary Dogfood

This file was created by the real Aezy Agent — not a subagent, not a
simulation — to verify the authoritative post-Turn changed-files summary.

Purpose:

- Confirm that a single `write` tool call to a new file is reported
  exactly once in the post-Turn changed-files summary.
- Confirm that the summary lists this file's exact path:
  `doc/dogfood/turn-summary-dogfood.md`.
- Confirm that no other files are modified by this turn.
