# M4.1A Modern Review Side Panel 签收记录

> 初次签收：2026-08-22；docked/accordion 交互复验：2026-08-23；Turn summary 与 Review surface 视觉复验：2026-08-24
> DSH runtime：`0.1.1-rc.1`
> Host：真实 Aezy profile，初次验收 `http://127.0.0.1:3091`，本轮复验 `http://127.0.0.1:3090`

## 边界与上游复核

M4.1A 完全位于参考树外的 `@aezy/project` 中，没有修改
`.local/deepseek-harness/`，也没有复制 DSH Session、Task、Subagent 或 conversation
scroll 内核。

开工时重新检查了最新公开 tag `dsh-v0.1.1-rc.2`
（`b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`，tree
`53915efe4e2126cc7779b73dfc8a3bcec5318c44`）。rc.1 → rc.2 的
`ui-layout`、`ui-slots`、`ui-theme` 关键文件没有功能变化，`details` 仍是
`single/session` 且已由上游 Tool Details 占用，没有新增通用 panel router。

在 2026-08-23 的交互复验中，产品要求明确为“桌面 panel 必须占据主页面空间”。
因此桌面端现在使用公开 `details` single/session slot，让 DSH AppFrame 原生 details
column 负责布局与拖拽宽度；窄屏才使用公开 additive `shell.overlay` list slot。这个
选择会在当前 rc.1/rc.2 中 shadow 上游 Tool Details occupant，是已知兼容性代价；待
上游提供 generic/additive panel router 后应迁移，不能为规避冲突而复制 layout 内核。
Aezy transient review controller 严格携带 `sessionId + cwd`，Session/Worktree 切换立即
关闭不匹配的 surface，也不读取或复制上游私有 scroll-anchor 实现。

## Host 与数据契约

Working changes 与 Historical Turn snapshot 现在投影为同一 version 2 DTO：

- file metadata：path、old/new path、modified/added/deleted/renamed/binary status、
  additions/deletions、truncated；
- staged/worktree/turn parts；
- hunks 与 context/addition/deletion/meta lines；
- 每行 old/new line number；
- malformed、combined、超行数或超长行输入 fail closed，hunks 为空，只暴露最多
  64 KiB 的显式 raw fallback；
- working identity 包含 repository root、path、fingerprint；client selection 再包含
  sessionId/cwd；historical identity 包含 sessionId、repository root、Turn、path、
  snapshot object id。

历史 raw unified diff 继续是 ledger object 的持久权威事实，结构 DTO 在读取时投影，
绝不根据当前 worktree 重算。`/turn-review` 现在必须带 path，只读取一个文件 object；
一个 Turn 中其他文件 object 损坏不会阻止打开当前文件。快速切换由
`AbortController + generation + active target identity` 三重门控，迟到响应不能覆盖
新选择。

M1 的 `change`、fingerprint、concurrent、batch preflight、receipt 和 fail-closed
Undo/Redo 语义未改变。M4 另存 review status/oldPath，使 clean tracked modification
不会在 Review UI 中伪装成 added，并能展示 Turn 内 rename；rename 仍不可安全撤销。

## Client 行为

- Turn card 不再请求或内联展开整 Turn raw diff；只保留 `Edited N files`、总/逐文件
  `+/-`、Undo/Redo、Review changes，以及 partial/concurrent/binary/truncated 状态；
- Turn card chrome 复用 DSH code-block family 的 12px geometry、
  `markdown-code-block`/`markdown-code-block-banner` theme aliases 和 code font，但不再
  冒用上游 `md-code-block` DOM class；Aezy 使用语义明确的 `aezy-change-surface` /
  `aezy-turn-changes` class；文件行位于无分隔线 body，总统计使用
  `└ +A -D · N files` footer；
- Review 或文件行在右侧 panel 打开对应 Turn/file；桌面 panel 作为原生 details column
  占据页面空间，窄屏降级为全宽 overlay；
- Working 与 Historical 来源标签显式区分；
- changed-file navigation 纵向排列为单选 accordion；每个文件复用同一
  `aezy-change-surface` 与 code-block/banner aliases，文件 button 收敛到 32px；点击文件名
  后在其正下方展开内容，并且只请求当前文件；
- Turn summary 与 Review 文件行共用按扩展名投影的 16px 内联 SVG 图标，覆盖 code、
  config、document、image、style、terminal、data 与 generic 类型，不增加图标包或读取
  DSH 私有源码；
- unified renderer 提供 old/new gutter、增删 status gutter、sticky expanded-file banner、
  柔和增删背景，以及 added/deleted/renamed/binary/truncated/fallback 空状态；历史 Turn
  不再重复显示 file status/snapshot id、`Turn N` part header 或 raw `@@` hunk header，
  多 hunk 只用轻量间隔符和行号跳变表达边界；Working staged/worktree label 仍保留；
- 所有颜色使用 DSH semantic theme aliases；
- close/Escape 只关闭 transient surface；conversation view 不被卸载，垂直 scroll state
  由原有 view 保留。桌面横向 reflow 是 docked layout 的预期行为。

## 自动与真实 Host 验证

自动测试通过 13/13，覆盖：

1. working staged/worktree DTO 与 old/new 行号；
2. standard、no-newline、rename-only、binary、malformed、combined diff parser；
3. malformed fail-closed raw fallback；
4. Historical Turn object 在后续 worktree 漂移后 deep-equal；
5. path escape 拒绝；
6. rename status/oldPath 且 revertable 保持 false；
7. 多文件 Turn 中第二个 object 损坏时第一个仍可读取，证明单文件 lazy load；
8. M1 file/Turn Undo/Redo 与 drift/concurrent 边界回归；
9. client bundle 同时注册 desktop `details` 与 narrow `shell.overlay`、使用
   AbortController、theme aliases，且不再包含 `Close review` /
   `Loading Turn diff…` 内联路径；
10. changed-file navigation 使用纵向 `aria-expanded` accordion，只有选中文件挂载
    `data-aezy-expanded-file` diff 内容；
11. semantic source regression 拒绝重新引入 `className="md-code-block"`、可见 snapshot id
    与 raw hunk header，并锁定 Aezy surface classes 和 file-icon marker。

真实 rc.1 Host：

```text
AEZY_TEST_URL=http://127.0.0.1:3091 pnpm run test:m1:http
→ path-scoped Historical Review、batch Undo/Redo、request fence passed
```

## Aezy 自身 dogfood 与浏览器 QA

真实 DeepSeek Agent Session：`m4-review-qa-home`，Turn 1。

Agent 在当前 Aezy 仓库写入 `doc/dogfood/m4-review-panel.md`，经 M2 一次性 approval
运行 `pnpm test:m1`，结果 13 tests / 13 pass / 0 fail。该 Turn 的 ledger snapshot
随后作为浏览器 Historical Review 的真实数据源。

由于当前 Codex Desktop task 仍携带旧 WSL workspace URI，Windows computer-use helper
按安全边界拒绝初始化；这是 HANDOFF 已记录的 Desktop metadata 限制。验收改用
`/tmp` 中一次性的 Playwright Chromium 直接访问真实 3091 Host，没有 mock UI 或
伪造 DTO。初次 overlay 方案的浏览器证据保留在历史提交中。2026-08-23 按
docked/accordion 要求重新在真实 rc.1 Host 断言：

- 1440px viewport 中，打开 Historical Review 后 conversation center 从 `1160px`
  收窄为 `800px`，details column 宽 `359px`；关闭后恢复 `1160px`，证明不是悬浮层；
- docked 打开、拖拽与关闭前后 conversation `scrollTop` 均为 `391`，view 未被重挂载；
- 拖拽 DSH 原生 details separator 后 panel 从 `359px` 增至 `451px`；
- Historical Turn addition/deletion rows、old/new gutter 与 ledger snapshot 可见；
- Working Changes 文件行纵向排列，第一文件初始展开；点击第二文件后第一项
  `aria-expanded=false`、第二项为 `true`，展开内容紧接选中文件名且 active path 正确；
- 快速切换仍保持单文件 lazy request 与旧请求取消/响应隔离；
- 680×900 viewport 只出现宽 `680px` 的 overlay，不挂载 details surface；
- wide docked Historical、wide docked accordion、narrow overlay 三张真实 Host screenshot
  均完成；fallback Chromium 的 UI 字体 glyph 栅格化不完整，因此最终判定同时依赖
  DOM accessible text、尺寸、展开状态与交互断言。

2026-08-24 Turn summary 视觉复验继续使用真实 rc.1 Host 与同一 dogfood Session：

- Aezy Turn summary 与同页 DSH 原生 fenced `md-code-block` computed background 均为
  `rgb(27, 27, 28)`，banner 均为 `rgb(44, 44, 46)`，圆角均为 `12px`；
- banner padding 为 DSH 基线的 `9px 14px`，body font 为 `13px/22px`；
- wrapper 无额外 border，文件 body 与 footer 都读取 DSH semantic aliases；
- Undo/Redo 与 Review changes 仍在 banner action 区，Review 仍能打开 docked details；
- client bundle 回归锁定 Aezy semantic surface class、banner marker 和全部相关 theme
  aliases，明确拒绝 `className="md-code-block"`。

同日 Review surface follow-up 复验：

- `pnpm run build:m1` 与 `pnpm run test:m1` 通过（16/16），M2 11/11、M3 6/6、brand
  1/1 与 peer check 均通过；
- `pnpm run test:m0` 在真实 rc.1 profile 通过 composition、Web、Workspace、Session 与
  Aezy preset smoke；
- `AEZY_TEST_URL=http://127.0.0.1:3090 pnpm run test:m1:http` 通过 Git/非 Git ledger、
  path-scoped Historical Review、batch Undo/Redo 与 request fence；
- 3090 实际 `/plugins/@aezy/project/client.js` 与 checkout build 字节一致，并包含新的
  Aezy surface/file-icon/source markers；
- 当前 Codex task 的 Windows computer-use 初始化仍因 WSL `file:///home/...` URI 被拒，
  因而本 follow-up 没有伪造新的截图结论；浅/深主题与宽/窄交互继续由前次真实浏览器
  QA、DSH semantic aliases、responsive surface 注册以及本轮 bundle regression 共同覆盖。

## 后续顺序（本轮未实现）

1. **M4.1B File Tree + code/Markdown/image Preview**：复用同一 Aezy side panel，
   Changes/Turn/Handoff 均可跳转；
2. **M4.2 `@directory` / `@diff` + 当前 Session Contextual Ask**：继续复用上游
   `@file/@session` reference seam；
3. **真正 Side Chat**：等待 DSH Interactive Side Sessions/fork/merge-back 公开 seam
   成熟；Aezy 不建立第二套持久 Session 状态机。

第一版明确不包含 editor、stage/unstage、hunk apply、inline comments、PR review、
AST diff、Monaco、Side Session 或 merge-back。
