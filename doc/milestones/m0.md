# M0：外置 composition 与真实 profile dogfood

## 当前状态

**Complete（2026-08-20）。** 外置 composition、真实 profile smoke 与首次真实模型 Turn 均已签收。整个验收使用发布版 DSH 和本机 Aezy profile，没有以 mock model 代替端到端运行。

已验证：

- 发布版 `@deepseek-ai/dsh@0.1.0-rc.7`，不是参考树源码运行时；
- `dsh plugin --profile aezy` 安装两个 Aezy 外置 bundle；
- profile 层顺序为 `dsh-base → aezy/base → dsh-web-app → aezy/web`；
- telemetry、feedback、动态 Cordis、Workflow/Ralph 默认禁用；
- Aezy 自有 Agent preset 是新 Session 的默认 composition；
- 真实 DSH Web Host 和已构建 frontend 启动并通过 HTTP 探活；
- 通过真实 Remote API 创建当前仓库的 Workspace；
- 通过真实 Remote API 创建 Session，并成功挂载 `agentPreset: aezy`；
- 通过真实 Remote API 驱动 `deepseek-official/deepseek-v4-flash` 完成一次 8-step Agent Turn；
- Agent 读取仓库约束、修改参考树外文件、执行 shell 验证并正常到达 `turn/end: completed`；
- `reference/deepseek-harness/` 未被导入、生成或修改。

## 签收记录

真实 dogfood 会话为 `m0-dogfood-20260820`，工作目录是仓库根目录，`agentPreset` 为 `aezy`。该 Turn：

1. 读取 `AGENTS.md`、`README.md` 和本文件；
2. 仅修改本文件，没有触碰 `reference/deepseek-harness/`；
3. 运行 `git diff --check` 与 `git status --short`；
4. 返回最终回复，并以 `turn/end` 的 `completed` 原因结束；
5. 由人工复核 diff 后提交，形成从真实 Agent 修改到 Git 审阅的完整闭环。

## 命令

```bash
pnpm install
pnpm run profile:sync
pnpm run test:m0
pnpm run aezy:web -- --host 127.0.0.1 --port 3080
```

Provider credential 应通过 Web 的 Models 设置写入 DSH 管理的 credential/settings 文档，不提交到仓库，也不要粘贴到测试日志。

## 实际 Agent Turn 证据

以下内容由上述真实 Agent Turn 自己追加，并由人工依据 session history 和 Git 状态复核：

1. **使用 Aezy preset**：本会话注入的 persona 文本与 `.local/dsh/.agent-presets/aezy/agent.cordis.yml` 逐字一致（`You are Aezy, a focused coding agent powered by the {{model}} model. Your working directory is {{cwd}}.`），且 `.local/dsh/.agent-presets/aezy/preset.yml` 中 `order: 0` 表示默认 preset；本 turn 确由 Aezy preset 组成。
2. **读取仓库说明**：通过 read 工具读取了 `AGENTS.md`、`README.md` 和 `doc/milestones/m0.md`，并按其中约束执行。
3. **修改该文件**：仅在本文件末尾追加本小节，未修改其他 tracked 文件，也未触碰 `reference/deepseek-harness/`。
4. **运行验证**：执行 `git diff --check`（无空白错误）与 `git status --short`（仅显示本文件为修改项）；按要求未执行 `git commit`，本修改保留在工作区待人工审阅。
