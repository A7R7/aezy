# M1：Workspace & Changes

## 当前状态

进行中。第一条可运行垂直切片建立在 DSH 公开 WebServer、client-module 与 UI slot seam 上，完全位于参考树外。

本阶段已定义并开始实现：

- Session cwd → Project → Git Repository → Local Environment 的最小权威链；
- branch/HEAD/upstream/ahead/behind 与 staged/unstaged/untracked/conflict 状态；
- 逐文件 staged/worktree diff；
- Web `Changes` 视图中的 repo header、changed-file list 与 diff view。

后续 M1 步骤：

1. 建立 turn-scoped change ledger，并在刷新/恢复后保持一致；
2. 以 ledger fingerprint 做 file/turn revert 的并发冲突拒绝；
3. 将 revert 做成可恢复操作并完成 Web 交互；
4. 用 Aezy 自身完成跨文件 dogfood、部分撤销和最终签收。

M1 不包含 stage/commit/push/PR、Worktree、文件树、用户 PTY 或新的 Session 内核。
