# M1：Workspace & Changes

## 当前状态

**Complete（2026-08-20）。** 核心实现、真实 rc.8 模型跨文件 Turn、Changes UI 部分撤销、浏览器刷新恢复和 receipt Undo 均已签收。全部实现建立在 DSH 公开 WebServer、`session/event`、client-module 与 UI slot seam 上，完全位于参考树外。

本阶段已定义并开始实现：

- Session cwd → Project → Git Repository → Local Environment 的最小权威链；
- branch/HEAD/upstream/ahead/behind 与 staged/unstaged/untracked/conflict 状态；
- 逐文件 staged/worktree diff；
- Web `Changes` 视图中的 repo header、changed-file list 与 diff view；
- 按 Session/Turn 持久化的观察式 change ledger；
- 每个已完成 Turn 最终回复下方基于真实 Git ledger 的 Changed files 摘要；
- 精确 fingerprint 冲突拒绝；
- 同时恢复 worktree/index 的 file revert，以及状态未漂移时可用的 Undo receipt。

已通过自动和真实 Host 验证：

1. staged/unstaged/untracked/conflict 与带空格路径解析；
2. 新 Ledger reader 能读取前一实例持久化的 Turn；
3. revert 恢复 Turn 前已有的脏 worktree/index，而不是粗暴回到 HEAD；
4. 新建文件撤销和 receipt Undo；
5. Turn 后文件漂移时拒绝覆盖；
6. 发布版 DSH profile 的真实启动图、client bundle、HTTP ledger/revert/undo 和 request fence。
7. `deepseek-official/deepseek-v4-flash` 在真实 Aezy Workspace Session 中读取约束、完成跨文件修改、执行 `git diff --check` 并以 `turn/end: completed` 结束；
8. 浏览器实际打开同一 Session 的 `Changes`，查看逐文件 diff，部分撤销一个文件，整页刷新后确认 receipt 与 Undo 持久，再 Undo 并刷新确认内容恢复。

## 签收记录

第一轮证据 Session 为 `m1-dogfood-rc8-20260820`，使用发布版 DSH rc.8、`agentPreset: aezy` 和 `deepseek-official/deepseek-v4-flash`。它在 8 个 step、15 次工具调用中读取 `AGENTS.md`、README、M1 与 brand 文档，只创建：

- `doc/dogfood/m1-runtime-observations.md`；
- `doc/dogfood/m1-safety-checklist.md`。

Turn 正常以 `completed` 结束；两个文件存在且非空，`git diff --check` 和新文件 whitespace 检查通过。Ledger 只记录这两个文件，`concurrent: false`，两者均 `revertable: true`。既有未跟踪 `test.md` 没有 Turn 归因，`.local/deepseek-harness/` 保持干净。

最终浏览器签收 Session 为 `m1-ui-dogfood-rc8-20260820`，绑定真实 Workspace `aezy-dsh-mvp`。模型在 5 个 step 中分别给上述两份文档追加 `UI 签收锚点`，并完成 `git diff --check`。真实 rc.8 Web 随后完成：

1. 侧栏选择该 Session，进入 `Changes`；
2. 确认 repo header 显示 3 个工作区变更、1 个 recorded turn；两份 Agent 文件标记 `T1`，用户 `test.md` 没有 Turn 标记；
3. 查看两个文件的逐文件 diff，选择 `m1-safety-checklist.md` 并点击 `Revert T1`；
4. 接受浏览器确认框后看到 recoverable receipt 和 Undo，另一文件保持修改；
5. 整页刷新并重新进入 Session/Changes：receipt 与 Undo 仍存在、撤销内容保持消失、二次 `Revert T1` 因 fingerprint 不匹配而禁用；
6. 点击 Undo，再次整页刷新：UI 锚点恢复、Undo 消失、`Revert T1` 恢复可用；
7. receipt `3b70e4ef-2acb-475e-90f9-bdf998d34f96` 最终持久状态为 `undone`。

真实 UI 同时显示 `A` + `Aezy` 的外置品牌 occupant，没有加载官方 DSH 品牌 occupant。完整 dogfood 产物保留在 `doc/dogfood/`，作为后续回归的人工可读基线。

当前不补 turn-level batch revert：在 M3 Worktree 隔离前，同一 checkout 的并发归因仍可能是 advisory；单文件 fingerprint 门控已经覆盖 M1 必需的安全撤销闭环，批量操作应等隔离所有权明确或出现可复现 dogfood 痛点后再评估。

Ledger 是对 Turn 边界前后仓库事实的观察，不把同一 checkout 中来自其他进程的修改谎称为 Agent Tool 作者行为。两个 Session 重叠时记录 `concurrent`；在 M3 Worktree 隔离前，这类记录不应自动批量撤销。symlink、非普通文件、conflict、rename 和超过 8 MiB 的待恢复文件当前 fail closed。

M1 不包含 stage/commit/push/PR、Worktree、文件树、用户 PTY 或新的 Session 内核。

## Turn 后已更改文件摘要（2026-08-21）

`@aezy/project` 现在以 `priority: -10` 占用 rc.8 公开的
`conversation.chat.turnTail` chain，在每轮最终 Assistant 回复下显示真实
Turn ledger 的文件摘要。首个 `/aezy/api/project/ledger` 请求会等待该 Session
异步 `turn/end` Git 扫描与原子写入结束，因此摘要不依赖客户端和 Host 的时序运气。

摘要最多显示 6 个文件 chip 和精确剩余数量；仍存在的文件复用 DSH Host
`openFile`，已删除文件显示删除态而不触发无效打开，同 checkout 有重叠 Session
时显示 `concurrent`。没有净文件变化的 Turn 不渲染行。数据源是 Turn 前后 Git
事实，不是工具名称或模型最终文本，因此覆盖 shell 修改/创建/删除，也不会把
同一 Turn 内最终恢复原状的文件误报为交付物。DSH `ui-deliverables` 没有删除或
修改；其系统提示与最终文本 inline file mentions 继续保留，只有工具推断的
Produced files 行被 Aezy 更权威的摘要优先占用。

真实 Agent dogfood 已在当前 rc.8 Aezy Host 上通过：Session
`turn-summary-dogfood-mt2be0rt` 按要求只调用一次 `write`，创建
`doc/dogfood/turn-summary-dogfood.md`；Turn 1 ledger 只列出该文件，返回的
`openPath` 是仓库内绝对路径，且没有把 Turn 开始前已存在的工作区改动归入本轮。
`pnpm run dogfood:turn-summary` 另提供可重复验收：每次新建临时 Git 仓库、驱动
真实 Aezy Agent Turn、核验精确单文件摘要，然后清理 fixture。

自动浏览器验收当前仍受 WSL → Windows computer-use 桥的 URI 校验限制：桥接层
拒绝 `file:///mnt/d/...` sandbox cwd。Host module、真实 Agent、真实 HTTP 和 client
bundle 路径均已验证；实际 light/dark theme 下的点击与布局仍列为人工验收项。
