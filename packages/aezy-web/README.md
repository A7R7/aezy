# @aezy/web

Aezy's out-of-tree policy layer over the published
`@deepseek-ai/dsh-web-app` bundle. It fixes the default Agent preset to Aezy's
trimmed coding composition and disables nonessential experimental or feedback
surfaces while retaining the DSH packages for reversible opt-in later.
